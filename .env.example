# Meta Conversions API — zkopíruj do .env (lokálně) a do Vercel → Settings → Environment Variables.
# .env se NEGITUJE (viz .gitignore).

META_PIXEL_ID=1541113114212456
META_CAPI_ACCESS_TOKEN=
# Jen pro testování (Meta Events Manager → Test events). V produkci proměnnou smazat/nenastavovat.
META_CAPI_TEST_EVENT_CODE=TEST66731
